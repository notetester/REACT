# npm start 기본값은 http://localhost:8080 이다.
# 다른 백엔드를 사용할 때 이 파일을 .env.development로 복사하고 값을 변경한다.
REACT_APP_API_BASE_URL=http://localhost:8080

# GitHub Pages처럼 백엔드 없이 둘러보는 정적 데모에서만 사용한다.
# 로컬 Oracle 연동 실습은 아래 값을 설정하지 않고 기본 real 모드를 유지한다.
# REACT_APP_API_MODE=mock
# REACT_APP_USE_HASH_ROUTER=true
